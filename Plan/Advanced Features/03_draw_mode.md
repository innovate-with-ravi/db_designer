Introduce the draw mode especially for only drawing ER. In this mode we can add infinitely many attributes directly attached to an entity.

And no need to specify data type and size but we can can't generate SQL or DDL from the ER