* Use tailwind's capabilities and make every CSS to not repeat so that it follows DRY principle.
  * Defined all repeated CSS once such as entity size and attribute size etc. 
  * It will also be useful in defining theme@ theme. We can use CSS variables.

* ctrl+z & ctrl+y
* ctrl+c & ctrl+v

* select tool to select by drawing, so that we can drag whole of the selected part

* hovering over components gives `title (small text)` by passing prop `title = ?`
