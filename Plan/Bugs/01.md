* handle connecting a same attribute multiple times to an enitity

<!-- * when canvas contains unary relationships, on clicking [] fit-view it doesn't fits properly. Maybe bcz react-flow doen't treats edges as nodes or custom edges as canvas items. So it removes them from main view. 

* OR why can't we allow connections simple & fluid like drawing means instead of handling alignment by react-flow. Why user can't simply draw connection lines how they want like drawing using pen and react-flow simply converts them to most relevant shape -->

* Few more bugs: 
-- We need to know the position of two entities which are connecting in a relationship (maybe using their x, y coordinates). So that we can dynamically place maximum cardinality for both enities on their respective sides like [E1](maxCard)(minCard)------(minCard)(maxCard)[E2]

* how lines behave when users connect opposing sides of the same box.